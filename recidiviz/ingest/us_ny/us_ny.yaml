key_mappings:
  DIN (Department Identification Number): person.person_id
  Inmate Name: person.surname
  Sex: person.gender
  Date of Birth: person.birthdate
  Race / Ethnicity: person.race
  Custody Status: booking.custody_status
  Housing / Releasing Facility: booking.facility
  Date Received (Current): booking.admission_date
  Earliest Release Date: booking.projected_release_date

  # TODO: string format is blocked on Python 3 migration, so uncomment
  # after that
  #Aggregate Minimum Sentence: sentence.min_length
  #Aggregate Maximum Sentence: sentence.max_length
